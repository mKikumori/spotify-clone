specVersion: "v1beta"
serviceId: "spotify"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "spotify-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
